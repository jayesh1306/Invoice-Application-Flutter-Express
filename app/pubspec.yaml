name: testing
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  scoped_model : ^1.1.0
  dio : ^3.0.10
  image_picker: ^0.6.7
  cloudinary_sdk : ^2.0.0+5
  splashscreen: ^1.3.5
  photo_view: ^0.11.0
  flutter_downloader: 
  path_provider: ^2.0.1
  open_file: ^3.2.0
  permission_handler:
  cupertino_icons: ^1.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  assets:
    - assets/